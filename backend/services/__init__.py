"""Service layer helpers for backend business logic."""

from __future__ import annotations

# Intentionally left minimal; modules under this package should expose reusable
# functions (e.g., notifications) that can be imported by blueprints or the
# legacy monolith while we continue modularization.
