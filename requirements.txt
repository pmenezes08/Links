
Flask==2.2.5  # Web framework (compatible with flask-oauthlib)
Werkzeug==2.2.3  # Compatible with Flask 2.2.5 and flask-oauthlib
flask-oauthlib==0.9.6  # For X OAuth SSO
requests==2.31.0  # HTTP requests (used in OAuth callback)
requests-oauthlib>=0.6.2,<1.2.0  # OAuth support for requests (compatible with flask-oauthlib)
stripe==7.0.0  # Stripe payments
python-dotenv==1.0.0  # Optional: for managing env vars like API keys
pywebpush==1.14.0  # Web Push notifications
cryptography>=41.0.0  # Needed by pywebpush for VAPID
redis==5.0.1  # Redis caching for performance
PyMySQL==1.1.0  # MySQL database connector
Pillow==10.1.0  # Image optimization and compression
openai>=1.0.0  # OpenAI API for audio transcription and summarization
firebase-admin==6.2.0  # Firebase Admin SDK for FCM push notifications
