# =============================================================================
# C.Point Production Environment Variables
# =============================================================================
# Copy this file and fill in your actual values
# For Google Cloud Run, set these as environment variables or use Secret Manager
# =============================================================================

# -----------------------------------------------------------------------------
# Flask Configuration
# -----------------------------------------------------------------------------
FLASK_ENV=production
FLASK_SECRET_KEY=generate-a-secure-random-string-here

# -----------------------------------------------------------------------------
# Database Configuration (Google Cloud SQL)
# -----------------------------------------------------------------------------
USE_MYSQL=true
DB_BACKEND=mysql

# For Cloud Run with Cloud SQL connector:
MYSQL_HOST=/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME
# Example: MYSQL_HOST=/cloudsql/my-project:us-central1:cpoint-db

# For direct connection (e.g., from local or Compute Engine):
# MYSQL_HOST=CLOUD_SQL_PUBLIC_IP

MYSQL_USER=app_user
MYSQL_PASSWORD=your-database-password
MYSQL_DB=cpoint

# -----------------------------------------------------------------------------
# Redis Configuration
# -----------------------------------------------------------------------------
# Option 1: Google Cloud Memorystore (expensive but managed)
# REDIS_URL=redis://MEMORYSTORE_IP:6379

# Option 2: Redis Cloud (has free tier, recommended for cost savings)
# REDIS_URL=redis://default:password@redis-xxxxx.region.redis-cloud.com:port

# Option 3: Disable Redis (app works without it, just no caching)
# REDIS_URL=

# -----------------------------------------------------------------------------
# Stripe Payment Processing
# -----------------------------------------------------------------------------
STRIPE_API_KEY=sk_live_your_stripe_live_key
# Use sk_test_... for testing

# -----------------------------------------------------------------------------
# Push Notifications
# -----------------------------------------------------------------------------
# VAPID Keys for Web Push (generate at https://vapidkeys.com)
VAPID_PRIVATE_KEY=your-vapid-private-key
VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_CLAIM_EMAIL=your-email@example.com

# Firebase Cloud Messaging (for iOS/Android)
# Set this to the contents of your Firebase service account JSON, or path to file
FIREBASE_CREDENTIALS={"type": "service_account", ...}
# Or use a file path:
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/firebase-credentials.json

# -----------------------------------------------------------------------------
# Cloudflare R2 Storage (CDN for media files)
# -----------------------------------------------------------------------------
R2_ACCESS_KEY_ID=your-r2-access-key
R2_SECRET_ACCESS_KEY=your-r2-secret-key
R2_BUCKET_NAME=cpoint-media
R2_ENDPOINT_URL=https://YOUR_ACCOUNT_ID.r2.cloudflarestorage.com
R2_PUBLIC_URL=https://cdn.c-point.co

# -----------------------------------------------------------------------------
# AI/ML Services (optional)
# -----------------------------------------------------------------------------
XAI_API_KEY=xai-your-key-here
OPENAI_API_KEY=sk-your-openai-key-here

# -----------------------------------------------------------------------------
# OAuth (X/Twitter SSO)
# -----------------------------------------------------------------------------
X_CONSUMER_KEY=your-twitter-consumer-key
X_CONSUMER_SECRET=your-twitter-consumer-secret

# -----------------------------------------------------------------------------
# App URLs
# -----------------------------------------------------------------------------
# Your production domain
APP_URL=https://app.c-point.co
# Landing page domain (if separate)
LANDING_URL=https://c-point.co

# -----------------------------------------------------------------------------
# Misc
# -----------------------------------------------------------------------------
# Set to 'true' to require email verification
VITE_REQUIRE_VERIFICATION_CLIENT=false
