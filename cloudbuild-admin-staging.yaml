# Cloud Build config for C.Point Admin Web - STAGING
# Use: gcloud builds submit --config=cloudbuild-admin-staging.yaml ./admin-web

steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '--build-arg=VITE_API_BASE=https://cpoint-app-staging-739552904126.europe-west1.run.app'
      - '--build-arg=API_PROXY_ORIGIN=https://cpoint-app-staging-739552904126.europe-west1.run.app'
      - '--build-arg=API_PROXY_HOST=cpoint-app-staging-739552904126.europe-west1.run.app'
      - '-t'
      - 'gcr.io/$PROJECT_ID/cpoint-admin-staging:latest'
      - '.'
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/cpoint-admin-staging:latest']
  - name: 'google/cloud-sdk:slim'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'cpoint-admin-staging'
      - '--image=gcr.io/$PROJECT_ID/cpoint-admin-staging:latest'
      - '--region=europe-west1'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--set-env-vars=API_PROXY_ORIGIN=https://cpoint-app-staging-739552904126.europe-west1.run.app,API_PROXY_HOST=cpoint-app-staging-739552904126.europe-west1.run.app'

options:
  machineType: 'E2_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY
