# Playwright itself + the Chromium browser come pre-installed and version-matched
# in the base image (mcr.microsoft.com/playwright/python). Do NOT add playwright
# here — pip-installing it would risk a browser/library version mismatch.
flask>=3.0,<4
gunicorn>=21,<24
